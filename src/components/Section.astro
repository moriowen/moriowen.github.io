---
interface Props {
  title: string;
  href?: string;
  id?: string;
}
const { title, href, id } = Astro.props;
---

<section id={id}>
  <h2>
    {href ? <a href={href}>{title}<span class="arr" aria-hidden="true">&rarr;</span></a> : title}
  </h2>
  <slot />
</section>
